# litellm/proxy/allowlist.filter
# Matched against destination host (incl. CONNECT targets). FilterDefaultDeny=Yes,
# so ONLY hosts matching a line below are permitted.
(^|\.)github\.com(:443)?$
(^|\.)githubcopilot\.com(:443)?$
